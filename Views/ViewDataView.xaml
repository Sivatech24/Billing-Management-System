<Page x:Class="Billing_Management_System.Views.ViewDataView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:Billing_Management_System.Views"
      mc:Ignorable="d"
      Height="Auto" Width="Auto">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Controls -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Select Table:" 
                       VerticalAlignment="Center"
                       FontWeight="Bold"
                       Margin="0,0,10,0"/>

            <ComboBox x:Name="cmbTables"
                      Width="200"
                      SelectionChanged="cmbTables_SelectionChanged"/>

            <Button Content="Refresh"
                    Width="100"
                    Margin="10,0,0,0"
                    Click="Refresh_Click"/>
            <Button Content="Edit" Width="100" Margin="10,0,0,0" Click="Edit_Click"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="dgData"
                  Grid.Row="1"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"
                  Margin="0,10,0,0"/>
    </Grid>
</Page>
